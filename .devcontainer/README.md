# devcontainer for local tests

Note: until https://github.com/devcontainers/spec/issues/671 is resolved,
to do test builds with a local version of the feature, you may need to
symlink ../src into this directory.
