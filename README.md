# Devcontainer feature for gcloud CLI

This is an expanded version of siri404's gcloud CLI feature, adding
options to install most of the common optional components (emulators,
appengine support, etc.)
